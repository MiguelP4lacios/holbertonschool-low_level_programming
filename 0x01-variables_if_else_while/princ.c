#include <stdio.h>
#include <time.h>
#include <stdio.h>
/**
    * main - Entry point
      *
  * Return: Alway
 */
int main(void)
{
	int n;

	for (n = 'z' ; n > 'a'; n--)
		putchar (n);
	putchar ('\n');
				
	return (0);
}
